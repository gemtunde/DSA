//pop
//removes last item frm an array and
// it also returns the last item removed from the array
//it modifies the original array

//EXAMPLE 1

const numbers = [1,2,3,4,5]
// numbers.pop();
// console.log(numbers)

//EXAMPLE 2
// it also returns the last item removed from the array//
let lastItem = numbers.pop();
console.log(lastItem)